<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jianwei.suteraproject">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SingleKainActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".ViewCart" />
        <activity android:name=".TestingCheckout" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".AdminMenu" />
        <activity android:name=".AddKain" />
        <activity android:name=".OpenOrders" />
        <activity android:name=".DeleteActivity" />
    </application>

</manifest>